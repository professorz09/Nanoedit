# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.kiro
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local state
supabase/.temp
.branches

# Never commit Google service-account keys or any secret JSON
service-account*.json
*-service-account.json
*.sa.json
secrets/

# Replit config holds environment secrets (Foundry API key etc.) — keep local only
.replit

# Claude Code local settings may contain tokens — keep local only
.claude/settings.local.json
zipFile.zip
