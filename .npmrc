fund=false
audit=false
# Lower network concurrency — npm's "Exit handler never called!" crash
# (npm/cli#4028 and similar) correlates with resource contention during
# install on constrained build machines; capping concurrent connections
# reduces the peak file-descriptor/socket usage that tends to trigger it.
maxsockets=3
fetch-retries=5
fetch-retry-mintimeout=20000
